# VIBE 项目状态
> 由 /vibe 维护；新会话跑 /vibe 自动续接

- 当前阶段：方案确认（第二轮：首页打卡流体验优化，2026-09-12 启动）
- 项目目标（一句话）：HabitFlow（日常行为坚持记录软件）的界面美化 + 围绕「记录坚持、看见痕迹」主线的实用功能优化。第二轮聚焦首页打卡流（Dashboard + RecordDialog），三个方向：补实用小功能、交互顺手度、视觉细节。
- 验收标准：
  1. 现有习惯打卡/成就/日志功能无回归（浏览器全流程走查 + tsc --noEmit + vite build 通过）
  2. 界面优化项逐条截图对比验收（浅色+暗色）
- 范围边界（第一版不做）：云同步/后端恢复、数据导入之外的迁移改造
- 已定决策：
  - 2026-09-12 访谈：延续纯本地 IndexedDB + Capacitor APK，无后端无登录
  - 2026-09-12 用户澄清：**本应用是日常行为坚持记录软件，不是记账软件**。曾按「习惯+记账共存」实现了记账模块（提交 fe316f2），随后用户明确「移除记账，回归纯粹」→ 已 git revert（a0b559b）。代码保留在 git 历史可随时找回
  - DB_VERSION 固定为 2：因记账版曾升级本地库到 v2，回退版本号会导致已升级设备打不开库（db.ts 有注释）
  - 美化方向：先诊断后改；V1/V3/V4/V5/V7 已完成（提交 7e6de2c）
- 功能点待办：
  - [x] V1 习惯列表删除按钮收进编辑表单
  - [x] V2 设置页导入导出实体按钮组（随 revert 移除后已单独恢复，提交 0e3bf4d）
  - [x] V3 日历格子降饱和
  - [x] V4 成就即时评估修复 + 未解锁样式
  - [x] V5 统计「每周目标」空态文案
  - [x] V6 暗色模式习惯图标底色区分度（透明度 22→2E）
  - [x] V7 已完成习惯条目对比度
  - [x] F-A 打卡附言：打卡时可附一句话，习惯详情/日历可见（提交 30415f5）
  - [x] F-B 统计页年度打卡热力图（GitHub 风格，自动定位最近日期）
  - [x] F-C 成就分享卡片：统计页分享按钮 → Canvas 品牌卡片 → 保存 PNG
  - [x] Icon 组件对未知图标名兜底渲染（修复 HMR 瞬态崩溃的健壮性加固）
- 第二轮待办（首页打卡流优化，2026-09-12 访谈选定）：
  - [ ] R-A 跳过保护：标记「跳过」不断连续天数（数据层 is_skipped + streak 三态逻辑 + 卡片徽章与入口）
  - [ ] R-B 全完成庆祝时刻：今日全部完成时全屏彩带庆祝 + 完成文案
  - [ ] R-C 昨日遗漏提醒：首页顶部提示昨天未完成项，一键进入补签
  - [ ] R-D 每日激励语：hero 卡片内置语料库按日期轮换（第一版不做自定义）
  - [ ] R-E 列表重排平滑动画：打卡后卡片换位平滑过渡，防闪跳误触
  - [ ] R-F 长按快捷菜单：详情 / 编辑习惯 / 跳过 / 补签
  - [ ] R-G 快速输入防丢+预填：失焦自动保存、预填上次数值
- 下一步：新开窗口 `/vibe T1`（跳过保护数据层）；T1 集成通过后再开 `/vibe T2`（首页体验包）。本窗口留作主控做集成审查
- 分区总览：T1 跳过保护数据层（待实现）· T2 首页打卡流体验包（待实现，依赖 T1）· 依赖：T2 → T1；接口合同已冻结在两卡内
- 已确认决策（第二轮）：任务分级=分区两卡多窗口；跳过日不计入完成率分母；先 Web 验收缓打包 APK；数值步进快捷键本轮不做
- 交接摘要：
  - 2026-09-12：记账模块完整实现后又按用户决定整体移除（fe316f2 → revert a0b559b）；习惯侧优化 V1~V7 + F-A/F-B/F-C 全部完成。
  - 2026-09-12 APK 交付：`HabitFlow-release-v6.apk`（versionCode 5 / versionName 1.4，3.35 MB），签名指纹 de94b020… 与历史版本一致可覆盖安装且保留数据；SHA-256 `3b32bd8ae5870475b34cc44beb876f24effdf6817b0d76c9d82397899c9becc1`。
  - 打包环境重建记录：本机原 Android Studio/SDK 已不存在（环境变量为残留）；新装命令行 SDK 于 `D:\Android\Sdk`（cmdline-tools + platform 36 + build-tools 36.0.0）；`frontend/android/keystore.properties`（storePassword/keyPassword=habitflow123，keyAlias=habitflow，storeFile 相对 app 模块为 ../../../android/keystore/habitflow-release.keystore）与 `local.properties` 已重建，均被 gitignore 排除不入库。
  - 注意：打包用 `gradlew assembleRelease` 需 `JAVA_HOME=D:\JDK`（系统 JAVA_HOME 指向已卸载的 Studio jbr，会污染 apksigner）。
