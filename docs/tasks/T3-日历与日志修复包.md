# T3 日历与日志修复包

- 状态：已集成
- 目标：修复第三轮诊断的 P1 两项（日历切月列表脱钩、日志标题截断）+ P3 小修两项，并落地全局主按钮统一规则的第一处（Journal）。背景：2026-09-12 五页浅+暗走查诊断（本卡问题均有一手截图与代码位置证据）。
- 接口合同（规划窗口冻结，执行窗口不得擅改）：
  1. **B1 主按钮统一规则（全局样式合同第①处）**：页面/表单底部主 CTA 使用 `btn btn-lg btn-primary` 并占主导宽度（w-full 或 flex-1，按现布局最小改动）；并排的次要按钮（取消等）保持 `btn-ghost` 自适应宽度。验收口径：主次按钮一眼可分。
  2. **A1 切月选中规则**：shiftMonth 切月后 selected 必须跟随目标月——目标月含今天则 selected=todayISO()，否则为该月 1 日；「今天」按钮现有行为（回到本月+选中今天）不变。下方记录列表永远显示 selected 当日记录，不出现「视图月 ≠ 列表月」。
  3. 涉及的 API、数据结构零变更（纯视图层）。
- 涉及文件：
  - `frontend/src/pages/CalendarPage.tsx`：A1（shiftMonth，约 102-111 行）
  - `frontend/src/pages/Journal.tsx`：A2（顶部标题区，约 96-112 行）、A7（textarea 加 resize-none）、B1 条款①（保存日志按钮，167 行附近）
  - `frontend/src/utils/date.ts`：仅当 A1 需要新的日期工具（如「该月 1 日 ISO」）时允许追加，不得改动既有函数
  - 禁改：index.css、Habits/HabitDetail/HabitForm/RecordDialog/Heatmap/Dashboard、local/、api/、types.ts（index.css 归 T4 范围外、其余归 T4，避免两卡文件重叠）
- 验收标准：
  1. 日历页切到任意其他月：下方记录列表标题的年月 = 当前视图年月；切回本月时选中日恢复为今天（浏览器操作验证）
  2. 390×844 视口、浅+暗双主题：日志页标题完整显示「2026年9月12日」无省略号，布局无换行错乱
  3. 日志 textarea 无 resize 手柄（style 含 resize: none）
  4. 「保存日志」呈大尺寸主按钮样式，与页内次要按钮主次分明
  5. `npm run build`（含 tsc --noEmit）通过；日历页与日志页浅+暗走查无回归（切日、编辑保存、补录等既有功能正常）
- 交接摘要：（执行窗口完成后填：做了什么、怎么验证的、发现的问题与建议）
  - 2026-09-12（T3 执行窗口）：**四点全部落地，状态「待集成」**（2 提交 08a31ba / e7ce94c）。做了什么：A1 shiftMonth 补 selected 跟随（目标月=今月→today，否则该月 1 日，复用组件顶部 now/today，date.ts 零改动）；A2 标题区拆两行（标题独占行去 truncate + 副标题与日期导航同行），走查中发现桌面视口有效宽 375（滚动条占 15px）下副标题截 7px，追加微调 gap-2→gap-1 + 日期输入 w-36→w-[136px]（两个环境均完整，date input 无内部截断）；A7 textarea resize-none；B1① 保存按钮 `btn btn-lg btn-primary w-full`（逐字对齐 ShareCard 合规写法）。怎么验证的：tsc --noEmit + vite build 全绿（chunk 警告为既有）；code-reviewer 自审 P0/P1 零、1 条 P2（重排后 p 缺 min-w-0 致 truncate 失效）当场修复、3 条 P3 为既有项未越界修；浏览器 390×844 走查：A1 全路径（9→10 月列表跟随 2026-10-01、切回 9 月恢复今天、跨年 2026-09→2027-01 得 2027-01-01、今天按钮不变式）；日历回归（切日 11/17 日、补录面板展开、非 boolean 项弹 RecordDialog）；日志回归（前一天/后一天正确加载相邻日内容、保存 toast「日志已保存」）；浅+暗双主题截图副标题完整（truncated:false）。发现的问题与建议：①既有：日历切日/点格子不重置补录面板（adding），面板跟随新日期显示未打卡项——行为自洽近似连续补录特性，但无明确定义，交规划/T0 定夺；②既有：日志页 entry 有内容时按钮仍显示「保存日志」而非「更新日志」（has_entry 判定，本卡未触碰该逻辑），建议 T0 走查确认是否 bug；③P3 既有未修：CalendarPage 跨午夜 today/now 闭包过期、快速切月 loadMonth 无 abort 竞态——建议留后续轮。
- 发现的问题：（跨模块/接口级问题，交规划窗口决策，不擅自改）
  - ①（执行窗口留痕，T0 审查确认属实但非本卡范围）：日志页 entry 有内容时按钮仍显示「保存日志」——根因在数据层 `local/api.ts` journalApi.get 兜底对象与 IDB stored 记录均不携带 has_entry 字段（client.ts:70 强转绕过类型检查），运行时恒 undefined；IDB 实查 demo 种子与新建记录皆无该字段。T3 禁改 local/，修复交规划窗口决策（建议：journalApi.get 补 `has_entry: !!j`、update 返回值同步补齐，与 dashboard 口径 `!!journal` 对齐，一行级改动）
  - ②（执行窗口留痕）：日历切日/点格子不重置补录面板（adding）——行为自洽近似连续补录，语义交规划窗口定夺
  - ③（执行窗口留痕，P3 既有）：CalendarPage 跨午夜 today/now 闭包过期、快速切月 loadMonth 无 abort 竞态——留后续轮
- 审查记录（T0 任务级，2026-09-13，**通过 → 已集成**）：
  - 接口合同符合性：B1① `btn btn-lg btn-primary w-full` 与 ShareCard.tsx:174 逐字符一致（CSS 中 btn-primary 本身 @apply btn，主次分明成立）；A1 selected 跟随逻辑与合同第 2 条逐条对齐（目标月=今月→today，否则该月 1 日），复用组件顶部 today/now 与既有 useState 初始化同源；API/数据结构零变更，禁改清单零触碰（diff 仅 CalendarPage.tsx +5 行、Journal.tsx 重排+样式、两份文档）
  - 验收标准逐条：①切月列表跟随实测过（9→10 月列表 2026-10-01、回 9 月恢复 2026-09-13、跨年 2027-01-01、今天按钮不变式）②390×844 浅+暗截图标题/副标题完整（DOM 数值 truncated:false，视口有效宽 375）③textarea computed resize:none ④保存按钮浅+暗双主题大尺寸主样式占满宽度 ⑤tsc --noEmit 单跑 CLEAN + npm run build 全绿（chunk 警告为既有）
  - 回归：日志页前一天/后一天双向切换正确加载相邻日内容；日历切日（点 9-11 格子）列表跟随、记录加载、补录入口在位
  - 执行窗口自审 4 条 P3 留痕处置：见上方「发现的问题」①②③；B1 四处集中复审按 VIBE.md 约定待 T4 完成后进行
