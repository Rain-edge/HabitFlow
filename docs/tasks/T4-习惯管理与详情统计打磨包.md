# T4 习惯管理与详情统计打磨包

- 状态：已集成
- 目标：第三轮诊断的文案/表单/图表/长列表打磨七项（A3-A5、A8-A9、B2-B3）+ 全局主按钮统一规则的其余三处。背景：2026-09-12 五页浅+暗走查诊断（一手截图与代码位置证据）。
- 接口合同（规划窗口冻结，执行窗口不得擅改）：
  1. **B1 主按钮统一规则（全局样式合同第②-④处，与 T3 第①处同一规则）**：HabitForm 底部（创建习惯/保存修改）、RecordDialog 底部（完成记录/保存修改）、HabitDetail 编辑记录弹窗底部（保存修改）主 CTA 用 `btn btn-lg btn-primary` 占主导宽度（w-full 或 flex-1）；并排取消按钮保持 `btn-ghost` 自适应。ShareCard「保存图片」已是 `btn-lg btn-primary w-full`，勿动。验收口径：四处主按钮与取消主次分明、观感一致。
  2. **A4 「每天」特判**：weekly_days 含全部 7 天时频次文案显示「每天」（现显示「每周一、二、三、四、五、六、日」）。Habits.tsx（23-25 行本地函数）与 HabitDetail.tsx（59-60 行内联）两处需统一——允许抽到 utils 新增共享纯函数（不改动既有导出）。
  3. **A5 分类规则**：新建默认 category 由 "general" 改为「日常」（HabitForm.tsx:44 初始值与 77 行兜底同步）；分类输入加 datalist 联想（选项=现有 habits 的 category 去重非空列表），保留自由输入能力；编辑既有习惯时原值原样保留。
  4. **B3 语义修正**：Heatmap LEVELS 现状为「无计划灰 / 计划 0% 红(danger-500/30) / 青色三档」——0% 档的红色是警示语义，不得删除；本项仅做观感弱化（如降饱和/降透明度），使浅色下与青色档协调且仍可区分。若执行窗口判断现状更优，需在交接摘要说明理由并维持现状。
  5. 数据层、API、types 零变更；A8 分组为纯渲染层（已有记录数组按 record_date 月份分组渲染）。
- 涉及文件：
  - `frontend/src/pages/Habits.tsx`：A4、A9（搜索 placeholder 缩短，完整文案在 390 宽下不截断）
  - `frontend/src/components/HabitForm.tsx`：A5、B1 条款②
  - `frontend/src/pages/HabitDetail.tsx`：A4、A8（历史记录按月分组，月份标题分隔，可选 sticky）、B2（数值/时长趋势图：YAxis 加 domain 聚焦数据范围 + 有 target_value 时加 ReferenceLine 目标虚线，recharts）、B1 条款④（编辑记录弹窗按钮）
  - `frontend/src/components/RecordDialog.tsx`：B1 条款③
  - `frontend/src/components/Heatmap.tsx`：A3（末月标签被裁——内层补 padding-right 使「9月」完整）、B3
  - `frontend/src/utils/`：仅允许新增共享纯函数文件或追加导出（A4），不得改动既有函数
  - 禁改：index.css、CalendarPage.tsx、Journal.tsx（T3 范围）、Dashboard.tsx、local/、api/、types.ts
- 验收标准：
  1. 全选 7 天的习惯在列表页与详情页均显示「每天」；部分选择（如每周一、三）文案不变
  2. 新建习惯弹窗：分类默认「日常」；输入时出现既有分类联想；编辑旧习惯分类原值保留
  3. 390 宽下搜索 placeholder 完整显示
  4. 详情页历史记录列表按月分组，滚动时可见月份标题；记录数据与分组前一致（条数不变）
  5. 数值型习惯（坚持喝水）详情趋势图：Y 轴聚焦数据范围（波动清晰，不从 0 起）+ 2000 ml 目标虚线可见；无目标的习惯不画目标线
  6. 热力图最右月标签完整显示「9月」；0% 档与青色档在浅+暗下均可区分且观感协调
  7. 四处主按钮统一大尺寸主样式，与取消按钮主次分明
  8. `npm run build`（含 tsc --noEmit）通过；相关页面浅+暗走查无回归
- 交接摘要：（执行窗口完成后填：做了什么、怎么验证的、发现的问题与建议）
  - 2026-09-13（T4 执行窗口）：**T4 全部 10 条款落地，状态「待集成」**——A3/A4/A5/A8/A9/B2/B3 + B1②③④（7 功能提交 4cc4088→92be633 + 自审修复轮 f6b2121）。tsc/build 全绿；code-reviewer 自审 P0 零、P1×1（tickFormatter Math.round 抹平小数刻度）当场修、P2×1（分组注释与实际依赖不符）当场修、P3×4 既有项留痕见「发现的问题」。浏览器 390×844 浅+暗双走查：A4 实操（7 天全选→「每天」、部分选→「每周一、三」、详情页副标题同步）、A5（默认「日常」、datalist 三选项、编辑原值「健康」保留）、A9（placeholder 66px < 内宽 114px，旧文案 126px 确实截断）、A8（3 个月份标题新→旧、60 条记录条数不变）、B2（Y 轴刻度 1453-2439 聚焦不从 0 起、「目标 2000 ml」虚线+标签可见）、B3（图例计算样式 danger 红 alpha 0.15，浅+暗与青色档可区分协调）、A3+B1 四处主按钮（btn-lg btn-primary flex-1 40px 高主导、取消 ghost 自适应、与 ShareCard 观感一致）全过验。集成审查入口：`git diff 939f5b0..HEAD`。
  - 执行中留痕决策：**B1 条款③与④为同一组件**——HabitDetail 的编辑记录弹窗即 RecordDialog，改 RecordDialog.tsx 一处同时覆盖两条款（卡上分列是因为诊断时按页面视角计数）；HabitDetail 页内其余 btn-primary（顶部「打卡」、补签面板「记录该日」）不属 B1 弹窗底部主 CTA 规则，未动。**A3 走查追加修复**：pr-6 预留后「9月」仍在 containing block 右边界临界换行（686px 临界），补 whitespace-nowrap，溢出由滚动余量容纳（f6b2121）。**B3 验证口径**：demo 数据过去一年无「计划但 0%」真实格子（zeroRateCells=0），0% 档观感以图例块等价验证（格子与图例共用同一 LEVELS 数组与尺寸渲染），T0 如需可造数据复核。
- 发现的问题：（跨模块/接口级问题，交规划窗口决策，不擅自改）
  - （P3，code-reviewer）trend 空且无 target 时 `Math.min(...[])` 得 Infinity、yDomain 为 [NaN, NaN]——当前被 `trend.length > 1` 渲染守卫挡住不可达；建议日后放宽渲染条件时先补空数组兜底。**T0 审查：守卫实测存在（HabitDetail.tsx:185），不可达声明属实，留痕即可**
  - （P3，既有）weekly_days 空数组时 scheduleLabel 输出「每周」（无日子），旧两处实现同样；且 HabitForm 允许每周指定日模式全不选直接提交、无校验——建议规划轮决定是否表单校验「至少选一天」。**T0 审查：确认存在；缓解因素=新建默认预选 [0,2,4]（HabitForm.tsx:44），全不选需手动逐一取消；交规划定夺**
  - （P3，口径说明）datalist 联想在合同「去重非空列表」基础上额外排除了遗留默认值 "general"（与 Habits.tsx:63 既有分类筛选逻辑完全一致），理由：A5 本身就是把 general 换成「日常」，联想列表出现 general 会误导；请 T0/规划确认口径。**T0 审查：口径确认合理，予以追认**——与既有筛选逻辑一致且符合 A5 淘汰 general 的意图
  - （P3，既有）RecordDialog 编辑+删除确认态并存小号「取消」（撤销确认）与 btn-ghost「取消」（关弹窗），语义易混；该并存为本次之前的布局，本次仅改对齐方向未处理。**T0 审查：现场确认并存（两个同名「取消」）——交规划定夺（建议后续轮改文案如「撤销」）**
- 审查记录（T0 任务级 + B1 四处集中复审，2026-09-13，**通过 → 已集成**）：
  - 合同符合性：B1②③④ `btn-lg btn-primary flex-1` + 取消 `btn-ghost`（条款③④同组件 RecordDialog 的留痕决策合理，HabitDetail 页内非弹窗 btn-primary 不属规则范围认定正确）；A4 schedule.ts 新增共享纯函数签名干净（Pick<Habit,…>），两处消费统一；A5 初始值/兜底同步改「日常」、datalist 静默降级；B3 仅改 LEVELS[1] 透明度 /30→/15 红色语义保留；数据层/API/types 零变更、禁改清单零触碰、ShareCard 未动
  - B1 四处集中复审（含 T3 条款①）：Journal `btn btn-lg btn-primary w-full` · HabitForm/RecordDialog `btn-lg btn-primary flex-1` · ShareCard `btn btn-lg btn-primary w-full`——w-full/flex-1 均为合同允许选项，四处渲染观感一致（截图过验）；**风格注记：T4 两处缺 `btn ` 前缀，因 CSS 中 btn-primary 自身 @apply btn 渲染零差异，留后续轮统一**（记 VIBE.md 后续待办）
  - 验收 8 条逐条：①7 天全选列表+详情均「每天」、部分选「每周二、四、六、日」不变 ②默认「日常」/联想含既有分类/编辑「健康」原值保留 ③placeholder「搜索习惯…」完整 ④3 个月份组新→旧、60 条不变 ⑤Y 轴 1453-2439 聚焦不从 0 起+「目标 2000 ml」虚线可见（无 target 由条件渲染保证不画线）⑥末月「9月」完整（right 323 < 容器右缘 342）+0% 档实测 rgba(229,72,77,0.15) 浅+暗与无计划灰/青色档色相可辨 ⑦四处主次分明 ⑧tsc 单跑 CLEAN+build 全绿+走查回归全过（RecordDialog 编辑保存 2090→2100 列表刷新、弹窗开关、详情浏览）
  - B3 造数复核：新建 7 天全选习惯当日未打卡 → 热力图出现真实 0% 格子（zeroCellCount=1），补齐执行窗口 demo 数据无 0% 格子的验证缺口
  - 走查勘误：初期疑「天按钮连点丢失」，实为新建表单 weekly_days 默认预选 [0,2,4]（HabitForm.tsx:44）被 toggle 所致，产品行为自洽无缺陷
  - 留痕 4 项处置：见「发现的问题」各条 T0 批注（1 追认口径、1 留痕即可、2 交规划）
