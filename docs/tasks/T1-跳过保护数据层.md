# T1 跳过保护数据层

- 状态：待集成（2026-09-12 执行窗口完成实现与验证，待主控集成审查）
- 目标：为打卡记录引入「跳过」状态——用户状态不好可标记跳过，不打卡但不断连续天数。本卡只做数据层 + 纯逻辑 + 统计口径，不做任何 UI（UI 由 T2 消费）。背景：用户连续天数是核心激励，状态差的日子被迫断签会打击坚持意愿。

- 接口合同（冻结，T2 依赖这些对外形态）：
  1. **存储**：`StoredRecord` 新增 `is_skipped: boolean`（`local/db.ts`）。旧记录无此字段一律视为 `false`，**不升级 DB_VERSION**（保持 2：不加新索引，记录本就全量内存过滤）
  2. **记录 API**（`api/client.ts` 门面形态不变）：
     - 创建跳过：`api.post("/records", { habit_id, record_date, is_skipped: true })` → 生成 `is_completed=false`、无数值的跳过记录
     - 取消跳过：`api.delete("/records/{id}")`（与现有删除语义一致，删除后该日期可重新记录）
     - 同一 habit+date 至多一条有效记录：对已有记录的习惯执行跳过时，由 T1 决定置位或替换，但必须保证结果满足「is_completed=false 且 skipped 生效」；取消跳过后该日期回到可重新打卡状态
  3. **查询 API**：`api.get("/records?on_date=YYYY-MM-DD")` 返回的 `HabitRecord` 含 `is_skipped`（T2 靠它排除昨日已跳过项）；新增 `api.get("/records?habit_id=X&limit=N")` 按日期倒序返回该习惯最近 N 条（T2 快速输入预填用），两个参数可组合、均向后兼容
  4. **视图类型**（`types.ts`）：`HabitRecord.is_skipped: boolean`；`TodayItem.skipped_today: boolean`；`TodayItem.record.is_skipped: boolean`
  5. **streak 语义**（`local/habitLogic.ts`）：`dailyStreaks` 升级接受跳过集合，跳过日为连续性「桥接」——current 与 longest 都跨过跳过日延续（完成-跳过-完成算连续 3 天）；`weeklyGoalStreaks` 不变（weekly_count 习惯无跳过概念）
  6. **统计口径**（`local/stats.ts`，全部跳过感知）：
     - `todayDashboard`：跳过日不计入 `scheduled_count` 分母、不算 `done_count`、不算「待完成」；item 上带 `skipped_today`
     - `overview` / `trend` / `getHabitStats`：expected 分母排除跳过日，完成率只反映真实执行
     - `calendar` / `yearHeatmap`：跳过日视同 empty（不渲染失败色），`CalendarDay.status` **不新增枚举值**
     - `evaluateAchievements`：跳过不计为完成
  7. **导入导出**：`exportData`/`importData` 完整携带 `is_skipped`（旧备份无此字段导入后视为 false）
  8. 跳过入口的 UI 调用方式（T2 按 T1 的实现落地，形态以 T1 交接摘要为准）

- 涉及文件：
  - 修改：`frontend/src/local/db.ts`、`frontend/src/local/api.ts`、`frontend/src/local/habitLogic.ts`、`frontend/src/local/stats.ts`、`frontend/src/types.ts`
  - 新建：`frontend/src/local/habitLogic.test.ts`（或项目现有测试目录内等价文件，执行时探查测试设施后定）
  - 禁止触碰：`frontend/src/pages/`、`frontend/src/components/`（UI 归 T2）；`DB_VERSION` 数值
- 验收标准：
  1. `tsc --noEmit` 通过
  2. 新增纯函数测试覆盖并跑通：跳过日桥接 current/longest streak、跳过日排除分母、weekly_count 不受影响
  3. 回归抽查（浏览器）：创建跳过记录后，首页完成率分母变化正确、统计页/日历/详情无报错、成就评估正常
  4. 导出 JSON → 清库 → 导入，跳过状态保留
- 交接摘要：
  - **做了什么**：`StoredRecord` 新增可选 `is_skipped`（DB_VERSION 仍为 2）；`recordApi.create` 支持跳过——对已有记录执行跳过=原位置位转换（复用原 id、清数值、is_completed=false），对跳过占位记录正常打卡=原位替换（免先删后建，服务 T2 R-G），两条真实记录冲突保留 409，weekly_count 习惯创建跳过拒绝 400；`recordApi.update` 显式 `is_skipped:true` 强制满足「is_completed=false 且无数值」不变量、录入真实数值（非 null）或显式 `is_skipped:false` 清除跳过、仅改备注保持原状；`recordApi.list/get` 出口归一化 `is_skipped` 为 boolean，list 支持 `habit_id`（日期倒序）+`limit` 组合；`dailyStreaks` 增加可选 `skipped` 参数（跳过日桥接：完成-跳过-完成=连续 3 天，今天跳过计入 current）；stats.ts 新增 `skippedDatesFor`/`allSkippedDatesMap`，`computeHabitStats` 加可选 skipped 参数（分母剔除跳过日、streak 桥接），todayDashboard 跳过不计分母/done/待完成且 item 带 `skipped_today`，trend/calendar/yearHeatmap 分母排除跳过日（全跳过日自然落回 empty，未新增枚举），成就：跳过不计完成（full_month 不受桥接影响，streak 系成就可由桥接解锁）；通知 pending 与每日小结豁免跳过习惯；importData 对 `is_skipped` 归一化（旧备份→false），导出自然携带。
  - **怎么验证的**：`npm test`（vitest 3.2.7，新增 habitLogic.test.ts + stats.test.ts 共 14 用例全过：桥接 current/longest、分母排除、weekly_count 不受影响、weekly_days 回归）；`npx tsc --noEmit` + `vite build` 通过；浏览器全流程（dev server + 页内真实模块调用）：基线 83.3%(5/6) → 跳过 habit1 → 100%(5/5) 分母正确，置位转换复用 id、替换清标记、409、habit_id+limit 倒序、桥接保持 streak（跳过后 current 不归零）、日历 09-11 跳过日呈空态色、统计/详情/成就无报错，导出→清库→导入后 2 条跳过状态完整保留；自审（code-reviewer）发现 P1（update 置跳过不清数值）已修复并浏览器复验（P1/P2-5/P2-6/归一化/备注保留五条路径全过）。
  - **发现的问题与建议**：见「发现的问题」。T2 对接要点：跳过入口=`api.post("/records", { habit_id, record_date, is_skipped: true, note? })`；取消跳过=`api.delete("/records/{id}")`；跳过后重新打卡可直接 `api.put("/records/{id}", { value_* })` 或 post 替换；取最近记录预填=`api.get("/records?habit_id=X&limit=N")`（含跳过行，请按 `is_skipped` 过滤）；TodayItem 新增 `skipped_today`、`record.is_skipped`。
- 发现的问题（交主控决策，不擅自改的部分）：
  1. **既有 bug 已顺手修复（请主控重点审查）**：`habitLogic.ts scheduledDates` 与 `stats.ts isScheduledOn` 原用 JS `in` 判断 weekly_days（对 Set 查属性、对数组查索引，均为误用；系 Python `in` 移植走样），导致 weekly_days 习惯的 expected/完成率/streak 恒为 0（如演示数据「睡前阅读」完成率恒 0%）。已改为 `Set.has` / `Array.includes` 并补防回归测试。此修复使 weekly_days 习惯统计数字集体由错误的 0 变为真实值，属用户可见变化。
  2. **合同第 4 条偏差**：`HabitRecord.is_skipped` 声明为可选（?: boolean）而非合同的必填 boolean——设为必填会使 `pages/CalendarPage.tsx:266` 既有记录字面量编译失败，而 pages/ 是本卡禁区。运行时已在 `recordApi.list/get` 出口归一化为 boolean，T2 按 truthy 判断即安全。若主控希望转正为必填，请 T2 窗口顺手给该字面量补 `is_skipped: false` 一行。
  3. **full_month/full_quarter 成就不感知跳过**（合同字面语义「跳过不计为完成」）：月内出现跳过日则该月无法达成「全部应完成日都完成」。与 streak 系成就的桥接语义不一致，是否放宽交主控决策。
  4. **create/update 的跳过语义无自动化测试**：依赖 IndexedDB，纯函数设施覆盖不到，已由浏览器回归覆盖（验收 3）；如需自动化建议后续引入 fake-indexeddb（新增依赖，未擅动）。
  5. **通知豁免超出合同清单**：`notificationApi.pending` 与每日小结将跳过习惯排除在「待完成」外（合同未列，符合功能意图，已实现并验证）。
